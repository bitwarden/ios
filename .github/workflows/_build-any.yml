name: Build App

on:
  workflow_call:
    inputs:
      environment:
        description: "Environment"
        type: string

jobs:
  build:
    name: Build ${{ inputs.environment }}
    runs-on: macos-15
    steps:
      - name: Echo
        run: |
          echo "placeholder"

