import BitwardenSdk
import SwiftUI

// MARK: - VaultListView

/// A view that allows the user to view a list of the items in their vault.
///
struct VaultListView: View {
    var body: some View {
        Text("Hello, world!")
    }
}
