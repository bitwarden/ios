// MARK: - ProfileSwitcherRoute

/// A route to a specific part of the profile switcher on iOS >=26 devices
///
public enum ProfileSwitcherRoute {
    /// A route to dismiss the profile switcher sheet.
    case dismiss

    /// A route to open the profile switcher sheet.
    case open
}
