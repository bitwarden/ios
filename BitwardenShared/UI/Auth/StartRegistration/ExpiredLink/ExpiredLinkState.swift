import SwiftUI

// MARK: - ExpiredLinkState

/// An object that defines the current state of a `ExpiredLinkView`.
///
struct ExpiredLinkState: Equatable {
    // MARK: Properties

    /// User's email address.
    var email: String = ""
    
}
