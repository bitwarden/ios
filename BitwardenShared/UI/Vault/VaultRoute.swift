// MARK: - VaultRoute

/// A route to a specific screen in the vault tab.
public enum VaultRoute: Equatable {
    /// A route to the add item screen.
    case addItem

    /// A route to the vault list screen.
    case list
}
