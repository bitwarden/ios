#include "./Common.xcconfig"
#include? "./Local.xcconfig"

ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon
PRODUCT_BUNDLE_IDENTIFIER = $(ORGANIZATION_IDENTIFIER).bitwarden.beta.watchkitapp
WK_COMPANION_APP_BUNDLE_IDENTIFIER = $(ORGANIZATION_IDENTIFIER).bitwarden.beta
PROVISIONING_PROFILE_SPECIFIER = $(PROVISIONING_PROFILE_SPECIFIER_WATCH_APP)
